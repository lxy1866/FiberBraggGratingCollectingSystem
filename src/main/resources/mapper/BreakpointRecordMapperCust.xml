<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.kaluna.modbusTcp.mapper.BreakpointRecordCustMapper">


<insert id="insertBreakpointInfoByScanPhysicalValue" parameterType="long">
    insert ignore into breakpoint_record (create_time, tag) select min(create_time) create_time, tag from physical_value where create_time > #{createTime, jdbcType=BIGINT}-1800000 and tag != 0 group by tag;
</insert>
</mapper>